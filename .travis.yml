language: php

sudo: required
dist: trusty
group: edge

cache:
    directories:
        - vendor
        - $HOME/.composer/cache

php:
    - 7.0
    - 7.1
    - hhvm

before_script:
    - composer install

script: phpunit
